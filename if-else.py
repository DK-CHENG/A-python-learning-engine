"""
条件判断学习 
目标：掌握if/elif/else的基础应用
"""

# ================================
# 1. 基础判断 - 网络端口识别
# ================================

port = 80  # 你抓包看到的端口号

print("端口分析:")
if port == 80:
    print("  这是网页服务的端口")
elif port == 443:
    print("  这是加密网页的端口")
elif port == 3389:
    print("  这是远程桌面的端口")
else:
    print(f"  端口 {port} 需要查资料")

# ================================
# 2. 组合条件 - 文件风险评估
# ================================

print("\n文件风险评估:")

is_exe = True      # 是不是exe文件
size_big = False   # 文件大不大

# and: 两个条件都要满足
if is_exe and size_big:
    print("  ⚠️  exe文件还很大，值得分析")
# or: 满足一个就行
elif is_exe or size_big:
    print("  📝 有一个特征，记下来")
else:
    print("  ✅ 看起来正常")

# ================================
# 3. 多层判断 - 外挂行为检测
# ================================

print("\n游戏外挂检测模拟:")

speed = 120     # 玩家移动速度
normal_speed = 50  # 正常速度

if speed > normal_speed:
    print("  速度有点快...")
    
    if speed > normal_speed * 2:  # 超过2倍
        print("  🚨 速度异常！可能开挂了")
    else:
        print("  ⚠️ 稍快，再观察下")
else:
    print("  ✅ 速度正常")

# ================================
# 4. 实际练习 - 简易恶意软件判断
# ================================

print("\n你的第一个安全判断:")

# 设置检测条件
file_type = "exe"        # 文件类型
has_virus_string = False  # 有没有病毒特征码

if file_type == "exe":
    print("分析exe文件中...")
    
    if has_virus_string:
        print("发现病毒特征！")
    else:
        print("没找到已知病毒特征")
        
        # 再检查一次
        check_again = input("要深入分析吗？(y/n): ")
        if check_again == "y":
            print("开始深入分析...")
        else:
            print("先标记，待会看")
            
else:
    print("暂时只分析exe文件")

# ================================
# 5. 逆向小技巧 - 条件调试
# ================================

print("\n调试技巧:")
debug_mode = True  # 打开调试模式

if debug_mode:
    print("  [调试] 正在检查内存...")
    print("  [调试] 发现可疑函数调用")
# 正常运行时这些不显示